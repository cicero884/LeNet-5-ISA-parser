#include<iostream>

// assume input is hax
// use stdin

union instruction;

int main()
{
	while(cin >> hax >> instruction.binary != EOF){
		switch(instruction.base.opcode){
			case layer_config:
				break;
			case transfer_config:
				break;
			case load_store:
				break;
			case conv:
				break;
			case pooling:
				break;
			default:
		}
	}
	return 0;
}
